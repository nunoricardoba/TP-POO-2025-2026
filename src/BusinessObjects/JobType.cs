namespace BusinessObjects
{
    /// <summary>
    /// Enum containing the jobs that a movie star can have
    /// </summary>
    public enum JobType
    {
        Unemployed,
        Director,
        Writer,
        Actor
    }
}
